@model IEnumerable<App_projekt_IT.Models.Service>

@{
    ViewData["Title"] = "Us³ugi";
}

<div>
    <h2>Zarz¹dzaj Us³ugami</h2>
    <div class="action-links">
        <a asp-action="Create" class="btn-primary" >Dodaj now¹ us³ugê</a>
    </div>
</div>

<div class="clinic-card" >
    <table class="table" >
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Name)</th>
                <th>Refundacja NFZ</th>
                <th >Akcje</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td >@Html.DisplayFor(modelItem => item.Name)</td>

                    <td >
                        @(item.IsNFZ ? "Tak" : "Nie")
                    </td>

                    <td class="action-links" >
                        <a asp-action="Edit" asp-route-id="@item.Id" >Edytuj</a> |
                        <a asp-action="Details" asp-route-id="@item.Id" >Szczegó³y</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id" class="warning-text">Usuñ</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>